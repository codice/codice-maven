// solr may not have shut down from the previous test, so a delay is nessecary to ensure it is fully stopped.
int delay = 4000
start = System.currentTimeMillis();

while((System.currentTimeMillis() - start) < delay){

}



